# Copia os NOMES para as "Environment Variables" do teu painel (Heroku, Railway, etc.).
# Preenche os VALORES no painel — NÃO commites ficheiros com senhas reais no Git.

# Obrigatório em produção:
# DATABASE_URL=postgres://USUARIO:SENHA@HOST:5432/NOMEDABD
# SECRET_KEY=uma_chave_muito_longa_e_aleatoria
# ALLOWED_HOSTS=meudominio.com.br,www.meudominio.com.br

# HTTPS (quase sempre necessário; usa o teu domínio com https://):
# CSRF_TRUSTED_ORIGINS=https://meudominio.com.br,https://www.meudominio.com.br

# Opcional:
# DEBUG=False
# SERVE_MEDIA=1
# WHITENOISE_NO_MANIFEST=1
